<?xml version="1.0" encoding="utf-8"?>

<!-- Material 3 Search Layout with CoordinatorLayout and MaterialCardView -->
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/launchpad_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Centered content wrapper for tablet support -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_horizontal">

        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="@integer/parent_layout_space_ratio" />

        <LinearLayout
            android:id="@+id/launchpad_main_layout"
            android:clickable="true"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="top"
            android:paddingTop="@dimen/parent_layout_padding_top"
            android:paddingBottom="@dimen/parent_layout_padding_top">

            <!-- Main Search Card - Glass Effect -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/search_card_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/extra_spacing"
                app:cardCornerRadius="@dimen/search_layout_radius"
                app:cardBackgroundColor="@color/glass_card"
                app:cardElevation="8dp"
                app:strokeWidth="0dp">

                <LinearLayout
                    android:id="@+id/search_card_layout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/large_spacing">

                    <!-- Search Bar - Floating Pill Shape -->
                    <com.google.android.material.card.MaterialCardView
                        android:id="@+id/search_bar_card"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/search_bar_height"
                        android:layout_margin="@dimen/large_spacing"
                        app:cardCornerRadius="50dp"
                        app:cardBackgroundColor="@color/glass_surface"
                        app:cardElevation="2dp"
                        app:strokeWidth="0dp"
                        android:stateListAnimator="@animator/search_bar_elevation">

                        <EditText
                            android:id="@+id/search_input"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:layout_gravity="start"
                            android:background="@android:color/transparent"
                            android:drawableStart="@drawable/baseline_search_24"
                            android:drawablePadding="12dp"
                            android:focusable="true"
                            android:focusableInTouchMode="true"
                            android:hint="@string/search_bar_input_text"
                            android:imeOptions="actionSearch"
                            android:inputType="text"
                            android:paddingHorizontal="@dimen/search_bar_input_padding"
                            android:singleLine="true"
                            android:textAlignment="viewStart"
                            android:textColor="@color/text_primary"
                            android:textColorHint="@color/text_hint"
                            android:textSize="16sp" />

                    </com.google.android.material.card.MaterialCardView>

                    <!-- Results RecyclerView with cascade animation -->
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/results_view"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/search_result_holder_height"
                        android:layout_marginTop="4dp"
                        android:orientation="vertical"
                        android:clipToPadding="false"
                        android:requiresFadingEdge="vertical"
                        android:fadingEdgeLength="16dp"
                        android:overScrollMode="never" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

        </LinearLayout>

        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="@integer/parent_layout_space_ratio" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>